<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.ford.persistenciasp.MainActivity">

    <!-- Opción para búsqueda -->
    <item
        android:id="@+id/action_search"
        android:orderInCategory="100"
        android:title="@string/action_search"
        android:icon="@drawable/ic_search"
        app:showAsAction="ifRoom" />

    <!-- Opción para compartir -->
    <item
        android:id="@+id/action_share"
        android:orderInCategory="100"
        android:title="@string/action_share"
        android:icon="@drawable/ic_share"
        app:showAsAction="ifRoom" />

    <!-- Opción para el color Azul -->
    <item
        android:id="@+id/action_blue"
        android:title="Blue"
        app:showAsAction="never" />

    <!-- Opción para el color Rojo -->
    <item
        android:id="@+id/action_red"
        android:title="Red"
        app:showAsAction="never" />

    <!-- Opción para el color Verde -->
    <item
        android:id="@+id/action_green"
        android:title="Green"
        app:showAsAction="never" />

    <!-- Opción para el color Amarillo -->
    <item
        android:id="@+id/action_yellow"
        android:title="Yellow"
        app:showAsAction="never" />

    <!-- Opción para el color Púrpura -->
    <item
        android:id="@+id/action_purple"
        android:title="Purple"
        app:showAsAction="never" />

    <!-- Opción para el color Naranja -->
    <item
        android:id="@+id/action_orange"
        android:title="Orange"
        app:showAsAction="never" />

    <!-- Opción para el color Cian -->
    <item
        android:id="@+id/action_cyan"
        android:title="Cyan"
        app:showAsAction="never" />

    <!-- Opción para el color Rosa -->
    <item
        android:id="@+id/action_pink"
        android:title="Pink"
        app:showAsAction="never" />

    <!-- Opción para el color Negro -->
    <item
        android:id="@+id/action_black"
        android:title="Black"
        app:showAsAction="never" />

    <!-- Opción para el color Blanco -->
    <item
        android:id="@+id/action_white"
        android:title="White"
        app:showAsAction="never" />

</menu>

